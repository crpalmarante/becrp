# BECRP — dependências Python
# Instalação: pip install -r requirements.txt
# Runtime principal: python3 server.py
#
# Nota WeasyPrint (DANFE/PDF): no Linux pode exigir libs do sistema
#   Debian/Ubuntu: sudo apt install libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf-2.0-0 libffi-dev shared-mime-info

# --- Core (server.py + certificado + SEFAZ / NFC-e / NFe / NFS-e / SPED) ---
lxml>=5.0.0
cryptography>=42.0.0
signxml>=4.0.0
qrcode>=7.4
Pillow>=10.0.0
weasyprint>=62.0

# --- Opcional: APIs FastAPI em modules/ (sales, purchase, procurement, notification…) ---
# Descomente se for subir esses serviços isolados:
# fastapi>=0.110.0
# uvicorn[standard]>=0.27.0
# pytest>=8.0.0
